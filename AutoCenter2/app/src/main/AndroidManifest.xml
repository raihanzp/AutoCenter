<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rzp.autocenter2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoCenter2">
        <activity android:name=".TidakDitemukan"></activity>
        <activity android:name=".pencarian" />
        <activity android:name=".HasilJual" />
        <activity android:name=".JenisKendaraan" />
        <activity android:name=".Tahun" />
        <activity android:name=".DeskripsiPage" />
        <activity android:name=".Register" />
        <activity android:name=".SignIn" />
        <activity android:name=".PageJual" />
        <activity android:name=".FirstPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>